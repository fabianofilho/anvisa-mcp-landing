---
const camadas = [
  {
    n: "01",
    titulo: "Dados",
    texto: "Os arquivos abertos de registro da Anvisa (medicamentos e dispositivos), baixados pelo sync. A Anvisa republica diariamente (D-1).",
  },
  {
    n: "02",
    titulo: "Armazenamento",
    texto: "Uma base DuckDB em arquivo (~42 MB). O sync é upsert: registro que sai da publicação fica marcado como não visto na última coleta.",
  },
  {
    n: "03",
    titulo: "Classificação",
    texto: "Um LLM local, compatível com a API da OpenAI, julga se um dispositivo usa IA. Quem grava os vereditos é a CLI; o servidor só lê.",
  },
  {
    n: "04",
    titulo: "Ferramentas MCP",
    texto: "Duas tools MCP, por stdio na sua máquina ou por HTTP no modo conector.",
  },
];
---

<section id="como-funciona" class="border-b border-neutral-900">
  <div class="mx-auto max-w-4xl px-6 py-20">
    <p class="text-sm font-semibold uppercase tracking-wider text-emerald">
      Como funciona
    </p>
    <h2 class="mt-3 text-2xl font-bold text-white sm:text-3xl">
      Quatro camadas, do dado bruto ao agente
    </h2>

    <!-- Diagrama: fluxo das 4 camadas -->
    <ol class="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">
      {
        camadas.map((c, i) => (
          <li class="relative rounded-xl border border-neutral-800 bg-ink-800 p-5">
            <div class="flex items-center gap-3">
              <span class="font-mono text-sm font-bold text-emerald">{c.n}</span>
              <span
                class="hidden text-neutral-700 lg:inline-block"
                aria-hidden="true"
              >
                {i < 3 ? "→" : ""}
              </span>
            </div>
            <h3 class="mt-3 text-base font-semibold text-white">{c.titulo}</h3>
            <p class="mt-2 text-sm leading-relaxed text-neutral-400">{c.texto}</p>
          </li>
        ))
      }
    </ol>

    <!-- Ressalva honesta, igual a documentacao tecnica -->
    <div class="mt-8 flex gap-3 rounded-xl border border-amber-500/25 bg-amber-500/5 p-4">
      <svg class="mt-0.5 h-5 w-5 flex-shrink-0 text-amber-400" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
        <path stroke-linecap="round" stroke-linejoin="round" d="M12 9v4m0 4h.01M10.29 3.86L1.82 18a2 2 0 001.71 3h16.94a2 2 0 001.71-3L13.71 3.86a2 2 0 00-3.42 0z" />
      </svg>
      <p class="text-sm leading-relaxed text-neutral-300">
        A classificação de SaMD com IA é <strong class="font-semibold text-white">heurística</strong>,
        gerada por um modelo de linguagem lendo nome técnico, nome comercial e
        fabricante (o registro não tem campo de descrição). Cada item traz confiança,
        justificativa e origem do veredito. Ajuda a triagem, mas não é fato regulatório
        e não substitui a consulta ao registro no portal da Anvisa.
      </p>
    </div>
  </div>
</section>
